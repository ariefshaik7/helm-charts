apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-web-app-nginx.fullname" . }}
  labels:
    {{- include "go-web-app-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-web-app-nginx.selectorLabels" . | nindent 4 }}
